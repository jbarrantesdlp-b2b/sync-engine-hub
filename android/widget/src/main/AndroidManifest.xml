<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".HourWidgetProvider"
            android:exported="true"
            android:label="@string/widget_hour_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hour_widget_info" />
        </receiver>

        <receiver
            android:name=".HubGlanceWidgetReceiver"
            android:exported="true"
            android:label="Sync Engine Hub Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hub_widget_info" />
        </receiver>
    </application>

</manifest>
